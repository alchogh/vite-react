import styled from "styled-components";

export default function DropDown({ option }: any) {
  return <Container>ghasdf</Container>;
}

const Container = styled.div`
  border: 1px solid black;
  padding: 40px;
`;

const MENU_TAP = [{ id: 1 }];
